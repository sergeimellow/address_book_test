(document).ready ->
  $('#new_book').validate
    rules:
      'user[book]':
        required: true
        book: true
      # 'user[password]':
      #   required: true
      #   minlength: 6
      # 'user[password_confirmation]':
      #   required: true
      #   equalTo: '#user_password'
  $('#edit_book').validate
    rules:
      'book[title]':
        required: true
        book: true
      # 'user[password]':
      #   required: true
      #   minlength: 6
      # 'user[password_confirmation]':
      #   required: true
      #   equalTo: '#user_password'